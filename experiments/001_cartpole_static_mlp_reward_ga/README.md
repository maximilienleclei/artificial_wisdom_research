# 001_cartpole_static_mlp_reward_ga

Static MLP genetic algorithm optimizing CartPole reward directly.

Run from this folder after installing `code/` into the active venv:

```powershell
C:\Users\Max\venv\Scripts\python.exe -m awr.experiments.cartpole_ga --generations 100 --population-size 64
```
